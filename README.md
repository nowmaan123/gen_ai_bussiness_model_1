# MarketMind

Starter repo for MarketMind: backend Flask API, frontend Streamlit app, services for preprocessing, modeling, scoring, forecasting, and content generation.

Run the backend:

```bash
python backend/app.py
```

Run the frontend:

```bash
streamlit run frontend/streamlit_app.py
```
